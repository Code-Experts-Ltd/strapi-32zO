'use strict';

/**
 * best-deals-heading service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::best-deals-heading.best-deals-heading');
