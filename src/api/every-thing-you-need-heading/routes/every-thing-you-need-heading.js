'use strict';

/**
 * every-thing-you-need-heading router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::every-thing-you-need-heading.every-thing-you-need-heading');
