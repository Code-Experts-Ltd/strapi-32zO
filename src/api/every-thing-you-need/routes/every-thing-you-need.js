'use strict';

/**
 * every-thing-you-need router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::every-thing-you-need.every-thing-you-need');
