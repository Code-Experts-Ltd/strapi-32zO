'use strict';

/**
 * subscribe-checkbox-text router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::subscribe-checkbox-text.subscribe-checkbox-text');
